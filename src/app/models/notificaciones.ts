import { DatosService } from '../datos.service';

export class notificaciones  {
  descripcion: string;
  estatus_leido: boolean;
  evento: string;
  rol_notificado: number;
  creado: any;
  creadopor: number;
}
